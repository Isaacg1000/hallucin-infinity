// Generic right-side slide-over. RoutePanel (the "Why this?" panel) builds on
// this directly; kept as a distinct, reusable primitive for any future
// contextual panel that needs the same behavior.
export { SidePanel as Drawer } from './SidePanel';
